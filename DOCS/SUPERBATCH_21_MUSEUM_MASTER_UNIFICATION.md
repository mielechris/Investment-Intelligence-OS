# Superbatch 21 — Museum Master 1.2 and Tuesday Factory unification

## Provenance

The presentation source is `IIOS Living Wall — The Auction Edition · Museum Master 1.2`, recovered from commit `6207944d9a6b53017c0175dcfaee7f585061225e` on `feature/iios-living-wall-77in-commissioning`. The current implementation is an additive port; it is not a merge or wholesale cherry-pick of the divergent historical branch.

## Unified architecture

`LivingWallApp` is the unified shell for Gallery, Story, Replay, Command, Expansion Wing, and Factory Watch. Gallery retains the three-floor, eighteen-room architectural cutaway and MAX's Walkway. The Expansion destination mounts the current `MobExpansionWing`, including the Tuesday Test Day Command Center, 24 product desks, 16 method desks, synthetic comparison sleeves, Candidate Conveyor, Professional Strategy Observatory, scoreboards, evidence inspection, and Post-Close Audit.

The synthetic sleeve basis is research-only and never summed into operational capital. Operational paper NAV and cash remain separate. Unknown values remain unknown.

## Truth and polling

`ExpansionWingSnapshotProvider` remains the only Expansion snapshot polling owner, at a minimum 15-second cadence. The Museum shell consumes its immutable same-origin snapshot context and creates no independent network loop. Browser navigation, room controls, fullscreen, brightness, and dialogs do not publish data or expose filesystem selection.

Motion requires a current, attributed candidate receipt with both immutable candidate identity and discovery timestamp. Aggregate counts, stale evidence, professional observations, and missing lineage cannot create movement. Unsafe authority freezes the scene. Quiet input produces `The House Is Quiet`.

## Display and accessibility

Wall Art Mode preserves safety truth while hiding controls. Fullscreen uses the browser Fullscreen API. Brightness is a bounded local display preference. Sound remains disabled pending an owned soundscape. Dialogs trap focus, close with Escape, and restore focus. Reduced-motion preferences disable animation without hiding architecture.

## Security boundary

The shell has no provider, credential, scanner, broker, ledger, order, publisher-control, or service-control route. It consumes sanitized scalar and bounded structured evidence only. Private 9I evidence, provider bodies, raw errors, credentials, and personal paths are prohibited.

## Activation and rollback

This batch is validated first on an isolated loopback fixture runtime. Permanent 5176 activation requires separate approval, clean-source verification, matching bundle hashes, a byte-for-byte rollback backup, and protected-service/paper-state comparison. Rollback replaces only the unified preview bundle and process; it never rewrites projections or touches Backend, Expansion preview, Factory Watch, publisher, Keychain, providers, brokers, or ledgers.

## Current limitations

Sound has no approved owned source and remains disabled. Motion remains quiet when the sanitized snapshot lacks current immutable event lineage. Full rendered acceptance remains a human gate when browser automation cannot capture pixels.
