import { useCallback, useEffect, useMemo, useState } from "react";
const API = "http://127.0.0.1:8002";
const ACTIVE_CASE_KEY = "iios.activeCaseId";
type DashboardCase = { case_id:string; topic:string; ticker?:string; health?:string; committee_disposition?:string; committee_confidence?:number; evidence_quality?:number; latest_return_pct?:number; latest_action?:string; monitoring_enabled?:boolean; };
type FactoryStatus = { portfolio?:{nav?:number|null;cash?:number|null;positions?:number|null;return_pct?:number|null;drawdown_pct?:number|null}; safety?:{all_invariants?:boolean;live_execution?:boolean} };
type History = { signal_ladder?:{current_stage?:string;next_requirements?:string[]} };
type State = { cases:DashboardCase[]; factory:FactoryStatus|null; history:History|null; online:boolean; caseId:string|null };
const pct=(v?:number|null)=>v==null?"—":`${(v*100).toFixed(1)}%`;
const money=(v?:number|null)=>v==null?"—":new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(v);
const label=(v?:string)=>String(v||"UNKNOWN").replaceAll("_"," ");
const tone=(v?:string)=>{const s=String(v||"").toUpperCase();if(/BROKEN|INVALID|REJECT|BLOCK|NO_TRADE|LOCK/.test(s))return"iios-state-block";if(/WATCH|PENDING|EARLY|WAIT/.test(s))return"iios-state-watch";if(/INTACT|CLEAR|QUALIFIED|APPROVED|READY/.test(s))return"iios-state-clear";return"iios-state-idle";};
export default function PortfolioThesisWarRoom(){
 const[state,setState]=useState<State>({cases:[],factory:null,history:null,online:false,caseId:window.localStorage.getItem(ACTIVE_CASE_KEY)});
 const load=useCallback(async()=>{const caseId=window.localStorage.getItem(ACTIVE_CASE_KEY);try{const[d,f]=await Promise.all([fetch(`${API}/monitoring/dashboard`),fetch(`${API}/factory-room/status`)]);if(!d.ok||!f.ok)throw new Error();const dashboard=await d.json() as{cases?:DashboardCase[]};const factory=await f.json() as FactoryStatus;let history:History|null=null;if(caseId){const h=await fetch(`${API}/history/${caseId}`);if(h.ok)history=await h.json() as History;}setState({cases:dashboard.cases||[],factory,history,online:true,caseId});}catch{setState(c=>({...c,online:false,caseId}));}},[]);
 useEffect(()=>{const initial=window.setTimeout(()=>void load(),0);const t=window.setInterval(()=>void load(),10000);return()=>{window.clearTimeout(initial);window.clearInterval(t);};},[load]);
 const active=useMemo(()=>state.cases.find(x=>x.case_id===state.caseId)??null,[state.cases,state.caseId]);
 const signal=state.history?.signal_ladder?.current_stage;const next=state.history?.signal_ladder?.next_requirements?.[0]||active?.latest_action||"Continue governed monitoring";const portfolio=state.factory?.portfolio;const locked=state.factory?.safety?.live_execution===false;
 return <section className="portfolio-war-room"><div className="portfolio-war-room-head"><div><div className="portfolio-war-room-eyebrow">PORTFOLIO & THESIS WAR ROOM</div><h2>{active?.ticker||"PORTFOLIO"} · <span className={tone(signal||active?.health)}>{label(signal||active?.health||"NO ACTIVE THESIS")}</span></h2><p>{active?.topic||"Select an active case to connect thesis state to paper portfolio context."}</p></div><div className={state.online?"portfolio-war-room-live":"portfolio-war-room-offline"}>{state.online?"WAR ROOM LIVE":"WAR ROOM OFFLINE"}</div></div>
 <div className="portfolio-war-room-primary"><div><span>THESIS SIGNAL</span><strong className={tone(signal)}>{label(signal)}</strong></div><div><span>COMMITTEE</span><strong className={tone(active?.committee_disposition)}>{label(active?.committee_disposition)}</strong></div><div><span>CONFIDENCE</span><strong>{active?.committee_confidence==null?"—":`${Math.round(active.committee_confidence*100)}%`}</strong></div><div><span>PORTFOLIO EXPOSURE</span><strong>{portfolio?.positions??"—"} positions · {money(portfolio?.nav)} NAV</strong></div><div><span>CAPITAL</span><strong className={locked?"iios-state-block":"iios-state-idle"}>{locked?"LIVE LOCKED":"VERIFY"}</strong></div></div>
 <div className="portfolio-war-room-grid"><div className="portfolio-war-room-panel"><div className="portfolio-war-room-label">PAPER PORTFOLIO</div><div className="portfolio-war-room-metrics"><div><span>NAV</span><strong>{money(portfolio?.nav)}</strong></div><div><span>CASH</span><strong>{money(portfolio?.cash)}</strong></div><div><span>POSITIONS</span><strong>{portfolio?.positions??"—"}</strong></div><div><span>RETURN</span><strong>{pct(portfolio?.return_pct)}</strong></div><div><span>DRAWDOWN</span><strong>{pct(portfolio?.drawdown_pct)}</strong></div></div></div><div className="portfolio-war-room-panel"><div className="portfolio-war-room-label">THESIS EVIDENCE</div><div className="portfolio-war-room-metrics"><div><span>SIGNAL</span><strong className={tone(signal)}>{label(signal)}</strong></div><div><span>COMMITTEE</span><strong className={tone(active?.committee_disposition)}>{label(active?.committee_disposition)}</strong></div><div><span>CONFIDENCE</span><strong>{active?.committee_confidence==null?"—":`${Math.round(active.committee_confidence*100)}%`}</strong></div><div><span>EVIDENCE</span><strong>{active?.evidence_quality==null?"—":`${Math.round(active.evidence_quality*100)}%`}</strong></div><div><span>CASE RETURN</span><strong>{pct(active?.latest_return_pct)}</strong></div></div></div></div>
 <div className="portfolio-war-room-decision"><div><span>NEXT GOVERNED REQUIREMENT</span><strong>{next}</strong></div><div><span>CAPITAL AUTHORITY</span><strong className={locked?"portfolio-war-room-lock":"iios-state-idle"}>{locked?"LIVE CAPITAL LOCKED":"VERIFY SAFETY STATE"}</strong></div></div></section>;
}
