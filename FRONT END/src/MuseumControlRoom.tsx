/* eslint-disable react-refresh/only-export-components -- registries are deterministic test seams */
import type { GovernedCase, AuctionModel } from "./auctionSceneModel";
import { useExpansionWingSnapshot, type ExpansionSnapshot } from "./ExpansionWingSnapshotContext";
import { latestFactoryOutputs, stationOutput } from "./museumLiveBinding";
import type { CaseFilter } from "./MuseumCaseLibrary";
import "./MuseumControlRoom.css";

type RecordValue = Record<string, unknown>;
type Department = "Market and Pipeline" | "Validation and Learning" | "Research and Professional Intelligence" | "Committee and Risk" | "Products, Methods and Sleeves" | "Projection and Provider Infrastructure" | "Evidence and Audit" | "Security and Authority";
type ControlPanel = { id: string; title: string; department: Department; section: string; scope: string; summary: string };

export const CONTROL_ROOM_PANELS: readonly ControlPanel[] = [
  { id: "executive", title: "Executive Factory Status", department: "Market and Pipeline", section: "service_health", scope: "OPERATIONAL READ-ONLY", summary: "The executive rollup reports only sanitized factory health." },
  { id: "calendar", title: "Market Session and Trading Calendar", department: "Market and Pipeline", section: "market_session", scope: "OPERATIONAL READ-ONLY", summary: "Session state comes from the approved market clock and calendar evidence." },
  { id: "services", title: "Protected Service Health", department: "Security and Authority", section: "service_health", scope: "OPERATIONAL READ-ONLY", summary: "Health is observational; this browser has no restart or repair route." },
  { id: "cadence", title: "Factory Operating Cadence", department: "Market and Pipeline", section: "last_cycle", scope: "OPERATIONAL READ-ONLY", summary: "Natural-cycle timing remains distinct from browser polling." },
  { id: "pipeline", title: "9A / 9B / 9E Pipeline", department: "Market and Pipeline", section: "radar", scope: "RESEARCH ONLY", summary: "Aggregate radar counts never create candidate identities." },
  { id: "validation", title: "9H Independent Validation", department: "Validation and Learning", section: "benchmark_9h", scope: "RESEARCH ONLY", summary: "Incomplete validation remains incomplete." },
  { id: "shadow", title: "9I Browser-Safe Shadow State", department: "Validation and Learning", section: "shadow_9i", scope: "OBSERVATIONAL ONLY", summary: "Only the compact browser projection is eligible for display." },
  { id: "outcomes", title: "9J Outcome Learning", department: "Validation and Learning", section: "outcomes_9j", scope: "RESEARCH ONLY", summary: "No outcome means no score, lesson, or profitability claim." },
  { id: "radar", title: "Radar Universe and Cycle Status", department: "Market and Pipeline", section: "radar", scope: "RESEARCH ONLY", summary: "Universe and cycle truth remain timestamped and sanitized." },
  { id: "lineage", title: "Immutable Candidate Lineage", department: "Market and Pipeline", section: "candidate_conveyor", scope: "RESEARCH ONLY", summary: "Candidate movement requires an exact current identity and discovery timestamp." },
  { id: "external", title: "Grok / Gemini Research Status", department: "Research and Professional Intelligence", section: "primary_source_review_queue", scope: "NOT ACTIVATED", summary: "External research status grants neither truth nor authority." },
  { id: "conveyor", title: "Candidate Conveyor", department: "Market and Pipeline", section: "candidate_conveyor", scope: "RESEARCH ONLY", summary: "The belt stops when lineage is missing, stale, or unsafe." },
  { id: "committee", title: "Investment Committee", department: "Committee and Risk", section: "committee", scope: "HUMAN GATED", summary: "Committee decisions cannot bypass evidence or risk controls." },
  { id: "skeptic", title: "Skeptic / Red Team", department: "Committee and Risk", section: "risk", scope: "HUMAN GATED", summary: "Opposing evidence and invalidation remain mandatory." },
  { id: "risk", title: "Risk Inspection", department: "Committee and Risk", section: "risk", scope: "HUMAN GATED", summary: "Missing loss, liquidity, sizing, or invalidation evidence fails closed." },
  { id: "paper", title: "Operational Paper Portfolio", department: "Committee and Risk", section: "books", scope: "OPERATIONAL READ-ONLY", summary: "The real paper fund remains separate from every synthetic sleeve." },
  { id: "reader", title: "Projection Receiving Office", department: "Projection and Provider Infrastructure", section: "projection_activation", scope: "OPERATIONAL READ-ONLY", summary: "Integrity, freshness, and underlying evidence are separate facts." },
  { id: "publisher", title: "Projection Publisher and Sequence", department: "Projection and Provider Infrastructure", section: "projection_activation", scope: "OPERATIONAL READ-ONLY", summary: "Browser requests cannot invoke publication." },
  { id: "provider", title: "Provider and Credit Status", department: "Projection and Provider Infrastructure", section: "provider_credit_meter", scope: "NOT ACTIVATED", summary: "Unknown accounting remains unavailable and provider authority stays false." },
  { id: "professional", title: "Professional Research Observatory", department: "Research and Professional Intelligence", section: "professional_strategy_observatory", scope: "ATTRIBUTED HYPOTHESES", summary: "Professional evidence cannot independently create or promote a candidate." },
  { id: "products", title: "Product Readiness", department: "Products, Methods and Sleeves", section: "tuesday_command_center", scope: "24 RESEARCH DESKS", summary: "Registration does not make a product operationally available." },
  { id: "methods", title: "Method Readiness", department: "Products, Methods and Sleeves", section: "method_manager_scoreboard", scope: "16 RESEARCH METHODS", summary: "Missing results stay null and insufficient-sample methods remain unranked." },
  { id: "sleeves", title: "Synthetic Sleeve Laboratory", department: "Products, Methods and Sleeves", section: "paper_research_sleeves", scope: "24 INDEPENDENT SLEEVES", summary: "Synthetic comparison basis — not deployable capital." },
  { id: "warehouse", title: "Evidence Warehouse", department: "Evidence and Audit", section: "knowledge_operations", scope: "METADATA ONLY", summary: "The browser receives scalar counts and fixed categories, never private evidence." },
  { id: "receipts", title: "Recent Sanitized Receipts", department: "Evidence and Audit", section: "last_cycle", scope: "READ-ONLY RECEIPTS", summary: "Long technical details remain collapsed and source text remains private." },
  { id: "postclose", title: "Post-Close Audit", department: "Evidence and Audit", section: "post_close_control", scope: "RECONCILIATION", summary: "Costs, outcomes, evidence gaps, and authority must reconcile before reporting." },
  { id: "security", title: "Security and Authority Locks", department: "Security and Authority", section: "authority_lock", scope: "FAIL CLOSED", summary: "Provider, credential, broker, ledger, order, and execution authority remain false." },
  { id: "blockers", title: "Current Blockers and Next Actions", department: "Validation and Learning", section: "tuesday_command_center", scope: "OBSERVATION SCHEDULE", summary: "Next actions are natural observations, never browser-triggered work." },
] as const;
export const CONTROL_ROOM_DEPARTMENTS: readonly Department[] = ["Market and Pipeline", "Validation and Learning", "Research and Professional Intelligence", "Committee and Risk", "Products, Methods and Sleeves", "Projection and Provider Infrastructure", "Evidence and Audit", "Security and Authority"];
export const CONTROL_TRUTH_STATES = ["CURRENT", "AVAILABLE", "AVAILABLE_EMPTY", "STALE", "INCOMPLETE", "UNAVAILABLE", "FAILED_CLOSED", "NOT_ACTIVATED", "RESEARCH_ONLY_UNPRICEABLE", "INSUFFICIENT_SAMPLE"] as const;
export const EFFECTIVE_TIMESTAMP_LABEL = "Effective timestamp";

const record = (value: unknown): RecordValue => value !== null && typeof value === "object" && !Array.isArray(value) ? value as RecordValue : {};
const scalar = (value: unknown): string => value === null || value === undefined ? "NOT REPORTED" : typeof value === "boolean" ? value ? "YES" : "NO" : typeof value === "string" || typeof value === "number" ? String(value) : "BOUNDED DETAIL";
const stateClass = (state: unknown) => scalar(state).toLowerCase().replaceAll("_", "-");
const humanState = (state: string, section: string) => {
  if (state === "CURRENT" || state === "AVAILABLE") return "Authenticated evidence is current";
  if (state === "AVAILABLE_EMPTY") return "Operating normally; no governed items are queued";
  if (state === "STALE") return "Last authenticated evidence is stale; advancement is blocked";
  if (state === "FAILED_CLOSED") return "Evidence rejected by the integrity gate";
  if (state === "NOT_ACTIVATED") return "Optional source is not connected";
  if (state === "UNAVAILABLE") return section === "service_health" ? "No authenticated source report received" : "Waiting for the next governed observation";
  if (state === "INCOMPLETE") return "Authenticated observation is incomplete";
  if (state === "INSUFFICIENT_SAMPLE") return "Waiting for enough governed observations";
  if (state === "RESEARCH_ONLY_UNPRICEABLE") return "Research observation exists; valuation is unavailable";
  return "No authenticated source report received";
};
function truth(snapshot: ExpansionSnapshot | null, section: string) { const value = snapshot?.sections?.[section]; return { state: value?.state ?? "UNAVAILABLE", data: record(value?.data) }; }
function evidenceTimestamp(data: RecordValue): unknown { return data.generated_at ?? data.observed_at ?? data.last_publication_time ?? data.projection_generated_at ?? null; }

function Panel({ panel, snapshot, overview }: { panel: ControlPanel; snapshot: ExpansionSnapshot | null; overview: unknown }) {
  const source = truth(snapshot, panel.section); const timestamp = evidenceTimestamp(source.data); const bound = stationOutput(panel.id, snapshot, overview);
  return <article className="museum-control-panel" data-module={panel.id} data-truth-tone={stateClass(bound.state)}><header><div><span>{panel.scope}</span><h3>{panel.title}</h3></div><strong className={`museum-control-state is-${stateClass(bound.state)}`}>{bound.state.replaceAll("_", " ")}</strong></header><p className="museum-control-value">{bound.value}</p><p className="museum-control-human-state">{humanState(bound.state, panel.section)}</p><p>{panel.summary}</p><dl className="museum-control-provenance"><div><dt>Source</dt><dd>{bound.source}</dd></div><div><dt>Evidence time</dt><dd>{scalar(bound.timestamp)}</dd></div><div><dt>Freshness</dt><dd>{bound.freshness}</dd></div><div><dt>Next observation</dt><dd>{scalar(bound.nextObservation)}</dd></div></dl><details><summary>Technical details and exact raw state</summary><dl><div><dt>State</dt><dd>{bound.state}</dd></div><div><dt>Evidence timestamp</dt><dd>{scalar(bound.timestamp ?? timestamp)}</dd></div><div><dt>Freshness</dt><dd>{bound.freshness}</dd></div><div><dt>Count</dt><dd>{scalar(bound.count)}</dd></div><div><dt>Provenance</dt><dd>{bound.source}</dd></div><div><dt>Eligibility</dt><dd>FALSE</dd></div><div><dt>Blocker</dt><dd>{scalar(bound.blocker)}</dd></div><div><dt>Event category</dt><dd>{scalar(bound.eventCategory)}</dd></div></dl><pre>{JSON.stringify({ source: bound.source, state: bound.state, timestamp: bound.timestamp, scope: panel.scope }, null, 2)}</pre></details></article>;
}
function ExecutiveCard({ label, value, state = "quiet" }: { label: string; value: string; state?: string }) { return <article data-executive-state={state}><span>{label}</span><strong>{value}</strong></article>; }

export default function MuseumControlRoom({ model, fixtureMode, selectCase, openCases, openProducts }: { model: AuctionModel; fixtureMode: boolean; selectCase: (item: GovernedCase) => void; openCases:(filter?:CaseFilter)=>void; openProducts:()=>void }) {
  const { snapshot, sanitizedOverview, connection, snapshotAgeSeconds, runtimeCapabilities } = useExpansionWingSnapshot();
  const publisherControl = runtimeCapabilities.publisherControl;
  void (publisherControl ? "UNSAFE" : "FALSE");
  const market = truth(snapshot, "market_session"); const conveyor = truth(snapshot, "candidate_conveyor"); const schedule = truth(snapshot, "tuesday_command_center"); const service = truth(snapshot, "service_health"); const registry=truth(snapshot,"governed_cases"); const caseCounts=record(registry.data.counts);
  const blockerStates = CONTROL_ROOM_PANELS.map((panel) => truth(snapshot, panel.section).state).filter((state) => !["CURRENT", "AVAILABLE", "AVAILABLE_EMPTY"].includes(state));
  const doingNow = model.motion.evidence ? "Moving one authenticated receipt through its governed room" : model.motion.ambient ? market.data.state === "MARKET_CLOSED_WEEKEND" ? "Market-closed watch: inspecting, filing, and waiting" : "Quiet-floor watch: observing approved sources without advancement" : "Safety lock engaged; all factory movement is frozen";
  return <main className="museum-control-room" aria-labelledby="museum-control-title"><header className="museum-control-heading"><span>COMMAND / COMPLETE SANITIZED OBSERVER</span><h1 id="museum-control-title">Museum Master Control Room</h1><p>Twenty-eight governed stations share one browser-safe snapshot. Missing fields remain unavailable; this room owns no publisher, provider, ledger, broker, or execution route.</p></header><section className="museum-now-strip" aria-label="What the factory is doing now"><span>WHAT THE FACTORY IS DOING NOW</span><strong>{doingNow}</strong><small>{model.motion.evidence ? "EVIDENCE-DRIVEN MOVEMENT" : model.motion.ambient ? "AMBIENT PRESENTATION ONLY" : "FAILED CLOSED"}</small></section><section className="museum-case-executive"><div><span>AUTHENTICATED CASE REGISTRY</span><strong>{scalar(registry.data.total)} total · {scalar(caseCounts.committee)} Committee · {scalar(caseCounts.risk)} Risk</strong><small>{scalar(caseCounts.without_browser_details)} incomplete details · freshness {scalar(registry.data.freshness_state)}</small></div><button onClick={()=>openCases("ALL")}>Open Case Library</button><button onClick={openProducts}>Open 24 Product Accounts</button></section><section className="museum-executive-layer" aria-label="Executive command layer"><ExecutiveCard label="Factory operating state" value={`${model.condition} / ${model.freshness}`} state={model.freshness.toLowerCase()}/><ExecutiveCard label="Market session" value={humanState(market.state, "market_session")} state={stateClass(market.state)}/><ExecutiveCard label="Protected-service health" value={humanState(service.state, "service_health")} state={stateClass(service.state)}/><ExecutiveCard label="Candidate Conveyor" value={humanState(conveyor.state, "candidate_conveyor")} state={stateClass(conveyor.state)}/><ExecutiveCard label="Operational paper NAV / cash" value={model.nav === null ? "Not reported / not reported" : `$${model.nav.toLocaleString()} / approved read model`} state="quiet"/><ExecutiveCard label="Primary blockers" value={blockerStates.length ? `${new Set(blockerStates).size} governed state categories` : "No reported blockers"} state={blockerStates.length ? "waiting" : "current"}/><ExecutiveCard label="Next scheduled factory action" value={humanState(schedule.state, "tuesday_command_center")} state={stateClass(schedule.state)}/><ExecutiveCard label="Authority" value={runtimeCapabilities.publisherControl ? "UNSAFE" : "READ ONLY · ALL LOCKS FALSE"} state={runtimeCapabilities.publisherControl ? "failed-closed" : "locked"}/></section><nav className="museum-control-index" aria-label="Control Room departments">{CONTROL_ROOM_DEPARTMENTS.map((department) => <a key={department} href={`#department-${department.toLowerCase().replaceAll(/[^a-z]+/g, "-")}`}>{department}</a>)}</nav><div className="museum-control-departments">{CONTROL_ROOM_DEPARTMENTS.map((department) => <section className="museum-control-department" id={`department-${department.toLowerCase().replaceAll(/[^a-z]+/g, "-")}`} key={department}><header><span>GOVERNED DEPARTMENT</span><h2>{department}</h2></header><div className="museum-control-grid">{CONTROL_ROOM_PANELS.filter((panel) => panel.department === department).map((panel) => <div id={`control-${panel.id}`} key={panel.id}><Panel panel={panel} snapshot={snapshot} overview={sanitizedOverview}/></div>)}</div></section>)}</div><section className="museum-output-feed" aria-label="Latest Factory Outputs"><header><span>LATEST FACTORY OUTPUTS</span><strong>BOUNDED · SANITIZED · DEDUPLICATED</strong></header>{latestFactoryOutputs(sanitizedOverview, 8).length ? <ol>{latestFactoryOutputs(sanitizedOverview, 8).map((item) => <li key={item.id}><time>{new Date(item.timestamp).toLocaleString()}</time><b>{item.module} · {item.category}</b><span>{item.state} · {item.freshness}</span><p>{item.explanation}</p>{item.opaqueIdentity ? <small>OPAQUE RECEIPT {item.opaqueIdentity}</small> : null}</li>)}</ol> : <p>No authenticated browser-safe output receipts were reported.</p>}</section><section className="museum-control-cases"><header><span>INTERACTIVE CASE THEATER</span><strong>{model.cases.length ? `${model.cases.length} GOVERNED CASES` : "NO AUTHENTICATED CASE DETAIL"}</strong></header>{model.cases.length ? model.cases.map((item) => <button key={item.id} onClick={() => selectCase(item)}><b>{item.ticker}</b><span>{item.thesis}</span><small>{item.id}</small></button>) : <p>The legacy event-derived theater has no complete case dossiers. <button onClick={()=>openCases("ALL")}>Open the authenticated 40-case registry instead.</button></p>}</section><p className="museum-control-footnote">Snapshot connection: {connection} · Age: {scalar(snapshotAgeSeconds)} · Fixture: {fixtureMode ? "NON-LIVE" : "FALSE"}</p></main>;
}
