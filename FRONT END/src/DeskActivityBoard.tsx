import { useEffect,useMemo,useState } from "react";
import { buildActivitySummary } from "./factoryActivityModel";
import type { RawLedgerEvent } from "./factoryLedgerAdapter";
const API="http://127.0.0.1:8002";type Status={activity?:{recent_events?:RawLedgerEvent[]}};
export default function DeskActivityBoard(){const[events,setEvents]=useState<RawLedgerEvent[]>([]);useEffect(()=>{let live=true;const load=async()=>{try{const r=await fetch(`${API}/factory-room/status`);if(!r.ok)return;const d=await r.json() as Status;if(live)setEvents(d.activity?.recent_events||[]);}catch{}};const initial=window.setTimeout(()=>void load(),0);const t=window.setInterval(()=>void load(),5000);return()=>{live=false;window.clearTimeout(initial);window.clearInterval(t);};},[]);const s=useMemo(()=>buildActivitySummary(events),[events]);return <section className="desk-activity-board"><div className="desk-activity-title"><span>EVENT-DERIVED DESK STATE</span><strong>No event = idle, never fake busy</strong></div><div className="desk-activity-grid">{s.desks.map(d=><div key={d.key} className={`desk-activity desk-activity--${d.state.toLowerCase()}`}><span>{d.label}</span><strong>{d.state}</strong><small>{d.lastEvent||"No recent desk event"}</small></div>)}</div></section>}
