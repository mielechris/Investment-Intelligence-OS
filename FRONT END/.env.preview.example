# Remote previews must use a separate, read-only API endpoint.
# Never place broker credentials or write-capable factory secrets here.
VITE_IIOS_API_URL=https://replace-with-read-only-telemetry.example
